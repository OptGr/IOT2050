"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const console_1 = require("console");
const fs = require("fs");
const path = require("path");
const utils_1 = require("../../api/utils");
const command_utils_1 = require("./command-utils");
let color = (0, command_utils_1.getColor)("magenta");
exports.default = (program) => {
    program
        .command("register-diagnostic")
        .alias("rd")
        .option("-c, --config <agentconfig>", "config file with agent configuration", "agentconfig.json")
        .option("-i, --agentid <agentid>", "agent id")
        .option("-k, --passkey <passkey>", "passkey")
        .option("-v, --verbose", "verbose output")
        .description(color("register agent for diagnostic *"))
        .action((options) => {
        (() => __awaiter(void 0, void 0, void 0, function* () {
            try {
                checkRequiredParamaters(options);
                const sdk = (0, command_utils_1.getSdk)(options);
                color = (0, command_utils_1.adjustColor)(color, options);
                (0, command_utils_1.homeDirLog)(options.verbose, color);
                (0, command_utils_1.proxyLog)(options.verbose, color);
                let agentid = options.agentid;
                const mcApiClient = sdk.GetMindConnectApiClient();
                if (options.config) {
                    const configFile = path.resolve(options.config);
                    const configuration = require(configFile);
                    agentid = configuration.content.clientId;
                    agentid || (0, utils_1.throwError)("invalid configuration in the file");
                }
                (0, command_utils_1.verboseLog)(`registering for diagnostic with agent id ${color(agentid)}`, options.verbose);
                yield mcApiClient.PostDiagnosticActivation(agentid);
                (0, command_utils_1.verboseLog)(`successfully registered the agent with agent id ${color(agentid)} for diagnostic`, true);
            }
            catch (err) {
                (0, command_utils_1.verboseLog)(color("This operation requires additionaly the service credentials."), options.verbose);
                (0, command_utils_1.errorLog)(err, options.verbose);
            }
        }))();
    })
        .on("--help", () => {
        (0, console_1.log)(`\n  Examples:\n`);
        (0, console_1.log)(`    mc rd -k mypasskey`);
        (0, console_1.log)(`    mc register-diagnostic --config someagent.json -passkey mypasskey`);
        (0, command_utils_1.serviceCredentialLog)();
    });
};
function checkRequiredParamaters(options) {
    !options.agentid &&
        !options.config &&
        (0, command_utils_1.errorLog)("you have to provide a filename for the agent configuration or assetid of the agent", true);
    !options.agentid &&
        options.config &&
        !fs.existsSync(options.config) &&
        (0, utils_1.throwError)(`the config file ${color(options.config)} doesn't exist.`);
}
//# sourceMappingURL=mc-register-diagnostic.js.map